#include <main.h>

/**
 * print_alphabet - a function that prints the alphabet, in lowercase
 *
 * Return: void
 */
int main(void)
{
	char n;

	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
